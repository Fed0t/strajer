services:
  strajer-server:
    build:
      context: .
      dockerfile: deploy/server/Dockerfile
    environment:
      RUST_LOG: ${STRAJER_RUST_LOG:-strajer_server=info,tower_http=info}
      STRAJER_BIND_ADDR: 0.0.0.0:8080
      STRAJER_HEALTHCHECK_ADDR: 127.0.0.1:8080
      STRAJER_JOIN_TOKEN: ${STRAJER_JOIN_TOKEN:?Set STRAJER_JOIN_TOKEN in .env}
      STRAJER_MAP_PATH: /maps/DotA_v6_89Q.w3x
    ports:
      - "${STRAJER_PUBLISH_ADDR:-127.0.0.1}:${STRAJER_PORT:-18080}:8080"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    volumes:
      - "${STRAJER_MAPS_DIR:-./maps}:/maps:ro"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    init: true
    restart: unless-stopped
